{
  "name": "shreedeliveryapps",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p www && cp -R css icons js index.html manifest.json www/ 2>/dev/null || true",
    "sync": "npm run build && npx cap sync android",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@capacitor/android": "^8.3.1",
    "@capacitor/core": "^8.3.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.3.1"
  }
}
